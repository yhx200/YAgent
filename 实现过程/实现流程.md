可以。我已经按你上传的方案，直接生成了一套 **YAgent 从零开始的完整 Starter 工程**，不是只给目录或伪代码。

这套工程保留了你方案里最重要的原则：**Runtime 只注册 Tool Schema/Proxy，不把下载的第三方 JS 直接 import 到 Agent 主进程；真正 Tool 代码放到独立 Node Runner/Sandbox 中执行。**  同时也按照你规划的 monorepo 结构组织了 `apps / packages / runtimes / tools / infra / docs`。

### 直接下载完整项目

[下载 YAgent 完整 Starter 项目 ZIP](sandbox:/mnt/data/yagent-full-starter.zip)

也可以先看项目里的完整中文启动教程：

[查看项目 README](sandbox:/mnt/data/yagent-full-starter/README.md)

整个项目目前有约 **93 个文件**，已经包含下面这些核心部分：

* `apps/yagent-platform`：Java 21 + Spring Boot + MyBatis + MySQL
* `apps/yagent-runtime`：Node.js + TypeScript Agent Runtime
* `packages/yagent-runtime-core`：Agent Runtime 抽象层
* `packages/yagent-runtime-adapter-deepseek`：DeepSeek Adapter
* `packages/yagent-tool-protocol`：`yagent-tool.json` 协议
* `packages/yagent-tool-sdk-node`：Node Tool SDK
* `runtimes/node-runner`：独立 Tool 执行进程
* `tools/demo-weather`：第一个演示 Tool
* `infra/docker-compose.yml`：MySQL / Redis / MinIO / Platform / Runtime / Runner
* `infra/sql/001_schema_and_seed.sql`：你设计的 8 张表 + 测试数据
* `runtime-data/tool-store/.../package.ytool`：已经打好的天气 Tool 包
* Capability Search / Acquire
* Tenant Installation
* Tenant Tool Policy
* Permission Check
* Tool Package Download
* SHA256 校验
* Session Scoped Tool Registry
* Tool Proxy
* Node Runner
* Audit Event

你之前设计的 `Capability → Tool → Version → Installation → Policy → Permission → Audit` 数据模型也保留在实现中，这正是原方案中能力和 Tool 分离的核心。

---

## 第一件事：解压项目

例如解压到：

```text
C:\Users\yhx\AgentLearning\yagent
```

进去以后应该看到：

```text
yagent
│
├── apps
│   ├── yagent-platform
│   ├── yagent-runtime
│   ├── yagent-gateway
│   └── yagent-web
│
├── packages
│   ├── yagent-common
│   ├── yagent-runtime-core
│   ├── yagent-runtime-adapter-deepseek
│   ├── yagent-tool-protocol
│   └── yagent-tool-sdk-node
│
├── runtimes
│   └── node-runner
│
├── tools
│   └── demo-weather
│
├── infra
│   ├── docker-compose.yml
│   └── sql
│
├── runtime-data
│
├── docs
│
├── scripts
│
├── package.json
├── pnpm-workspace.yaml
├── .env.example
└── README.md
```

---

## 第二件事：先跑 Mock 模式

为了让你作为小白可以先把整条链跑通，我专门做了：

```env
YAGENT_LLM_MODE=mock
```

所以第一遍**不需要 DeepSeek API Key**。

先复制：

```bat
copy .env.example .env
```

然后直接：

```bat
docker compose -f infra/docker-compose.yml --env-file .env up --build
```

它会启动：

```text
MySQL            3306
Redis            6379
MinIO            9000/9001

YAgent Platform  8080
YAgent Runtime   3000
Node Runner      8090
```

---

## 第三件事：测试三个服务

Platform：

```text
http://localhost:8080/actuator/health
```

Runtime：

```text
http://localhost:3000/runtime/health
```

Runner：

```text
http://localhost:8090/health
```

都正常之后再测试 Agent。

---

## 第四件事：第一次真正调用 Agent

Postman：

```http
POST http://localhost:3000/runtime/chat
```

Body：

```json
{
  "tenantId": 10001,
  "userId": 20001,
  "sessionId": "S001",
  "message": "帮我查询上海天气"
}
```

这次不是简单：

```text
用户
→ AI
→ 回答
```

而是真的经过：

```text
用户
 ↓
AgentRuntime
 ↓
发现需要天气能力
 ↓
capability_search
 ↓
Platform
 ↓
ya_capability
 ↓
weather.current
 ↓
capability_acquire
 ↓
ya_tool_capability
 ↓
com.yagent.weather
 ↓
ya_tenant_installation
 ↓
判断租户已安装
 ↓
ya_tenant_tool_policy
 ↓
Permission
 ↓
ToolMaterializer
 ↓
下载 package.ytool
 ↓
SHA256 校验
 ↓
解压到 Tool Cache
 ↓
Session ToolRegistry
 ↓
动态出现 weather_query
 ↓
下一 Agent Step
 ↓
weather_query
 ↓
Tool Proxy
 ↓
Node Runner
 ↓
真正加载 demo-weather
 ↓
weather.query
 ↓
返回结果
 ↓
ya_audit_event
 ↓
Agent 最终回答
```

预计返回：

```json
{
  "success": true,
  "data": {
    "sessionId": "S001",
    "reply": "查询完成：上海当前 26°C，晴。"
  }
}
```

这就已经实现了你文档里描述的核心动态能力链路：

```text
Capability Missing?
 ↓
Capability Resolver
 ↓
Capability Dictionary
 ↓
Provider
 ↓
Permission
 ↓
Materialization
 ↓
Tool Schema
 ↓
Next Agent Step
 ↓
Tool Proxy
 ↓
Sandbox Runtime
 ↓
Actual Tool
 ↓
Audit
```

这正是你原方案定义的 YAgent 主干。

---

## 然后再切真实 DeepSeek

等 Mock 跑通以后修改 `.env`：

```env
YAGENT_LLM_MODE=deepseek

DEEPSEEK_API_KEY=你的Key

DEEPSEEK_BASE_URL=https://api.deepseek.com

DEEPSEEK_MODEL=deepseek-chat
```

重新启动：

```bat
docker compose -f infra/docker-compose.yml --env-file .env down
docker compose -f infra/docker-compose.yml --env-file .env up --build
```

此时：

```text
MockAgentRuntimeAdapter
```

就会替换成：

```text
DeepSeekAgentRuntimeAdapter
```

而：

```text
CapabilityResolver
ToolManager
PermissionClient
ToolMaterializer
ToolRegistry
Sandbox
Audit
```

完全不用改。

这也是为什么项目里专门有：

```text
packages/yagent-runtime-core
packages/yagent-runtime-adapter-deepseek
```

两层。

---

有一点需要说明：我已经把工程、SQL、预制 `package.ytool` 和对应 SHA256 都生成好了，但当前执行环境无法访问 npm/Maven 网络仓库，所以这里**没有实际完成远程依赖下载后的完整编译测试**。代码结构和整条调用链已经落地，你在自己的 Windows/WSL 网络环境执行 `docker compose ... up --build` 后，如果出现第一个编译错误，把完整错误发给我，我们就从**第一个错误开始逐个修到整个项目真正跑通**。

你现在最适合做的就是：**下载 ZIP → 解压 → `.env` → Docker 启动 → 先打 `/runtime/health`。**

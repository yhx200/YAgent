<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yagent.platform.mapper.installation.TenantInstallationMapper">

    <select id="selectByTenantAndTool"
            resultType="com.yagent.platform.domain.installation.TenantInstallationDO">

        SELECT
        id,
        tenant_id AS tenantId,
        tool_id AS toolId,
        version,
        status,
        install_user_id AS installUserId,
        install_time AS installTime

        FROM ya_tenant_installation

        WHERE tenant_id = #{tenantId}
        AND tool_id = #{toolId}

        LIMIT 1

    </select>

</mapper>

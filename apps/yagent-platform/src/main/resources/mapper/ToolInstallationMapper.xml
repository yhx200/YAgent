<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yagent.platform.mapper.tool.ToolInstallationMapper">

    <resultMap id="BaseResultMap"
               type="com.yagent.platform.domain.ToolInstallationDO">

        <id column="id"
            property="id"/>

        <result column="tenant_id"
                property="tenantId"/>

        <result column="tool_id"
                property="toolId"/>

        <result column="version"
                property="version"/>

        <result column="status"
                property="status"/>

        <result column="create_time"
                property="createTime"/>

        <result column="update_time"
                property="updateTime"/>

    </resultMap>


    <select id="selectByTenantAndTool"
            resultMap="BaseResultMap">

        SELECT
        id,
        tenant_id,
        tool_id,
        version,
        status,
        create_time,
        update_time

        FROM ya_tool_installation

        WHERE tenant_id = #{tenantId}
        AND tool_id = #{toolId}

        LIMIT 1

    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yagent.platform.mapper.YaToolMapper"><select id="selectActiveByIds" resultType="com.yagent.platform.domain.YaTool">SELECT * FROM ya_tool WHERE status='ACTIVE' AND tool_id IN <foreach collection="toolIds" item="id" open="(" separator="," close=")">#{id}</foreach></select></mapper>

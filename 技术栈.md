```
Agent Runtime       → DeepSeek Harness，外面加自己的 Adapter
远程工具协议         → MCP
Yagent 自有工具协议   → JSON Schema 2020-12
平台服务             → Java + Spring Boot
运行时               → TypeScript / Node.js
数据库               → MySQL
缓存                 → Redis
工具搜索 V1          → MySQL
工具搜索 V2          → Elasticsearch / Qdrant
沙箱 V1              → Docker
沙箱 V2              → WASM/WASI + Container
对象存储             → MinIO / OSS / S3
签名                 → Ed25519
Hash                 → SHA-256
通信                 → HTTP/JSON-RPC
```
